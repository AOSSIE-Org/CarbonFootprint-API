const Logger = require('@framework/Logger');

const { getApiToken, getFullUserProfile, getFitData } = require('../services/googleFitServices');

exports.fitData = (req, res) => {
  const { userId } = req;

}; 